import { AdvancedDynamicTexture, Button, Control } from "@babylonjs/gui";
import { Engine, Scene, Vector3, Color4, FreeCamera } from "@babylonjs/core";

function guiMenu(engine): void { //Promise<Scene> {

    return;
}


export { guiMenu };